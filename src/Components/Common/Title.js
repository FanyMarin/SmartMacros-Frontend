import React from "react";

const Title = ({ title }) => {
  return (
    <h4 className="forms-title table-title uk-padding-small uk-light uk-text-uppercase uk-text-bold">
      {title}
    </h4>
  );
};

export default Title;
